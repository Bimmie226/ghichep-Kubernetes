# Install NFS-provisioner on K8s cluster with Helm 

## Install Helm 

Tham khảo [tại đây](https://github.com/Bimmie226/ghichep-Kubernetes/blob/master/Deploy_Application_on_K8s/06.Ingress_Kubernetes.md#install-helm)

## Install nfs-provisioner 

Thêm Helm repo cho nfs-subdir-external-provisioner 

```bash
helm repo add nfs-subdir-external-provisioner https://kubernetes-sigs.github.io/nfs-subdir-external-provisioner/
```

Cài đặt NFS Provisioner 

```bash
helm install nfs-subdir-external-provisioner nfs-subdir-external-provisioner/nfs-subdir-external-provisioner \
  --namespace nfs-provisioner \
  --create-namespace \
  --set nfs.server=192.168.174.114 \
  --set nfs.path=/data \
  --set nfs.mountOptions[0]=nfsvers=4.1 \
  --set storageClass.name=nfs-client \
  --set storageClass.defaultClass=false \
  --set storageClass.accessModes=ReadWriteMany \
  --set storageClass.reclaimPolicy=Delete \
  --set storageClass.volumeBindingMode=WaitForFirstConsumer
```

Trong đó: 

- `nfs.server`: IP hoặc hostname của NFS server 
- `nfs.path`: Folder được export từ NFS server 
- `storageClass.name`: Tên storageClass được tạo 
- `storageClass.defaultClass`: Tùy chọn có sử dụng storageClass được tạo ra có phải là storageClass mặc định không ?
- `storageClass.accessMode`: tùy chọn accessMode 
- `storageClass.reclaimPolicy`: tùy chọn Policy sau khi xóa PVC 
- `storageClass.volumeBindingMode`: Tùy chọn tạo PVC khi nào 


```bash
devops@lab-k8s-master-01:~/tools/nfs-provisioner$ helm install nfs-subdir-external-provisioner \
  nfs-subdir-external-provisioner/nfs-subdir-external-provisioner \
  --namespace nfs-provisioner \
  --create-namespace \
  --set nfs.server=192.168.174.114 \
  --set nfs.path=/data \
  --set nfs.mountOptions[0]=nfsvers=4.1 \
  --set storageClass.name=nfs-client \
  --set storageClass.defaultClass=false \
  --set storageClass.accessModes=ReadWriteMany \
  --set storageClass.reclaimPolicy=Delete \
  --set storageClass.volumeBindingMode=WaitForFirstConsumer
NAME: nfs-subdir-external-provisioner
LAST DEPLOYED: Fri Jun 26 07:03:24 2026
NAMESPACE: nfs-provisioner
STATUS: deployed
REVISION: 1
TEST SUITE: None
devops@lab-k8s-master-01:~/tools/nfs-provisioner$
```

# Tài liệu tham khảo 

[REFERENCE 1](https://cloud.vnpt.vn/tai-lieu/article/huong-dan-su-dung-nfs-lam-persistent-storage-cho-vnpt-cloud-k8s-416)